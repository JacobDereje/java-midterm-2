package linkedLists;

public class linkedlistvidexample {
     class Node {
        int data;
        Node next;
        Node(int data) {
            this.data = data;
        }
    }
    Node head = new Node(4);
    Node nodeB = new Node(2);
    Node nodeC = new Node(3);
    Node nodeD = new Node(10);


}
